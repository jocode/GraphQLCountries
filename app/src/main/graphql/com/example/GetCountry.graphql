query GetCountry($countryCode: ID!) {
    country(code: $countryCode) {
        code
        name
        capital
        emoji
        currency
        languages {
            name
        }
        continent {
            name
        }
    }
}